<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <ImplicitUsings>disable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <TargetFrameworks>net6.0;netstandard2.1</TargetFrameworks>
        <LangVersion>default</LangVersion>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
      <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
      <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    </PropertyGroup>

    <ItemGroup>
      <Reference Include="Buffer">
        <HintPath>libs\Buffer.dll</HintPath>
      </Reference>
      <Reference Include="GNumerics">
        <HintPath>libs\GNumerics.dll</HintPath>
      </Reference>
      <Reference Include="GText">
        <HintPath>libs\GText.dll</HintPath>
      </Reference>
      <Reference Include="Recycle">
        <HintPath>libs\Recycle.dll</HintPath>
      </Reference>
    </ItemGroup>

</Project>
